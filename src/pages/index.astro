---
import {contentPostWindowPaginator} from "../lib/content/contentPostWindowPaginator";
import BaseLayout from "../layouts/BaseLayout.astro";
import PostWindow from "../components/post-window/PostWindow.astro";
import Masonry from "../components/Masonry.astro";
const posts = await contentPostWindowPaginator(1);
---
<BaseLayout title="Olis.tech - Start" description="Code, Tux & Bytes: Ein Entwickler-Tagebuch">
  <Masonry>
    {posts.map((post) => (
      <PostWindow post={post} />
    ))}
  </Masonry>
  <!-- todo: next page -->
</BaseLayout>
