{
  "name": "@olis.tech/source",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "update": "nx migrate latest",
    "update-install": "pnpm install --no-frozen-lockfile",
    "migrate": "pnpm exec nx migrate --run-migrations",
    "build": "nx run web:build",
    "check": "nx run web:check",
    "preview": "nx run web:preview",
    "dev": "nx run web:dev",
    "sync": "nx run web:sync",
    "start": "npm run preview"
  },
  "private": true,
  "dependencies": {
    "@nxtensions/astro": "18.2.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.5.2",
    "@nx/js": "18.0.8",
    "@nx/workspace": "18.0.8",
    "@swc-node/register": "~1.8.0",
    "@swc/core": "~1.3.85",
    "@swc/helpers": "~0.5.2",
    "astro": "^4.3.5",
    "nx": "18.0.8",
    "prettier": "^2.6.2",
    "typescript": "~5.3.2"
  }
}
